---
apiVersion: v1
kind: Service
metadata:
  name: truenas
  namespace: traefik
spec:
  type: ExternalName
  externalName: "${STORAGE_HOST}"
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: truenas
  namespace: traefik
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`truenas.${CLUSTER_DOMAIN}`)
      kind: Rule
      middlewares:
        - name: authelia
          namespace: authelia
      services:
        - name: truenas
          port: 80
  tls:
    secretName: efym-net-tls
